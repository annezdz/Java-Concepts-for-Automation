package interfacepack;

public class InterfaceDescriptions {

    /*
    * Interface is nothing but the pure abstract class which contains only abstract methods
    *
    * Interface are always abstract whether we mentionit or not
    *
    * Declaration: In order to declare as interface the keyword "interface" is used
    * */
}
